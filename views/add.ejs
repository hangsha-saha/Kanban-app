<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Task</title>
    <style>
    body, h2, label {
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #b4b4b4;
      margin: 0;
    }

    .add-task-section {
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0px 2px 6px rgba(0, 0, 0, 0.1);
      max-width: 600px;
      width: 90%;
      margin: 20px auto;
      padding: 40px;
      text-align: center;
    }

    .add-task-form {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .add-task-form label {
      font-weight: bold;
      color: #333;
      text-align: left;
    }

    .add-task-form input,
    .add-task-form textarea {
      padding: 12px;
      border: 1px solid #ccc;
      border-radius: 4px;
      resize: none;
      font-family: inherit;
    }

    .add-task-button {
      padding: 12px 24px;
      background-color: #007bff;
      color: #fff;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-weight: bold;
      transition: background-color 0.3s;
    }

    .add-task-button:hover {
      background-color: #0056b3;
    }

    .back-button-container {
      text-align: center;
      margin-top: 20px;
    }

    a {
      text-decoration: none;
      color: #007bff;
      font-weight: bold;
    }

    a:hover {
      text-decoration: none;
    }

    @media screen and (max-width: 600px) {
      .add-task-section {
        padding: 20px;
      }
    }
    </style>
</head>
<body>
  <br><br><br>
  <div class="add-task-section">
    <h2>Add New Task</h2><br>
    <form action="/add" method="POST" id="addTaskForm" class="add-task-form">
      <label for="title">Title:</label>
      <input type="text" id="title" name="title" required>
      <label for="description">Description:</label>
      <textarea id="description" name="description" required></textarea>
      <button type="submit" class="add-task-button" id="btn-1">Add Task</button>
    </form>
    <br><br>
    <div class="add-task-button-container">
      <a href="/" class="add-task-button">Back</a>
    </div>
  </div>
  
</body>
</html>
